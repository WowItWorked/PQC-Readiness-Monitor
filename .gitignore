# Claude Code local settings (personal permissions — not shared)
.claude/settings.local.json

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# Dependencies (none today, but future-proof)
node_modules/
